doctype html
html(class="no-js", lang="ru")

    include _mixin
    include ../components/head/head
    include ../components/header/header
    include ../components/footer/footer
    include ../components/popup/popup

    head
        //- start  block head
        link(href="%=static=%css/separate-css/slick.css", rel="stylesheet", type="text/css")
        link(href="%=static=%css/separate-css/jquery.datetimepicker.css", rel="stylesheet", type="text/css")
        block head
        - if(headData)
            +head(headData)
        - else
            +head(head.defaults)
        //- end  block head
    body.page
        |%=symbols=%

        section.page__wrapper
            +header(index)
            -if(!breadcrumb)
                .container
                    br
                    br
                    ul.breadcrumb.not-styleLink
                        li: a(href="#"): span Главная
                        li: span О компании

            //- Include your components here
            //- start block content
            block content
            //- end block content


            .page__buffer
        .page__footer
            +footer()

        +popup()

        //a(href='#').linkTop.not-styleLink

        script(src='%=static=%js/separate-js/svg4everybody.min.js')
        script svg4everybody();

        script(src="%=static=%js/separate-js/bootstrap-select.min.js" type="text/javascript")
        script(src="%=static=%js/separate-js/inputmask.min.js" type="text/javascript")
        script(src="%=static=%js/separate-js/jquery.datetimepicker.js" type="text/javascript")
        script(src='%=static=%js/separate-js/dropdown.js')

        //- start block script
        block script
        //- end block script
        //- Main scripts. You can replace it, but I recommend you to leave it here
        script(src='%=static=%js/main%=hash=%%=min=%.js')

